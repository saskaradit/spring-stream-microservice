spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/saskaradit/spring-config
          clone-on-start: true
    fail-fast: true